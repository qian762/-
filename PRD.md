# 优品汇家居相册APP - 完整需求文档

## 一、项目概述

| 项目 | 说明 |
|------|------|
| 产品定位 | 优品汇家居相册与展示移动应用 |
| 核心场景 | 按分类 → 风格 → 照片三级结构组织家具照片，支持备注和分享 |
| 目标平台 | Android + iOS + Web（Expo 三端） |
| 部署方式 | 网页版，服务持续运行 |
| 用户体系 | 无登录注册，全局共享数据 |
| 照片存储 | Supabase Storage（云端对象存储） |

---

## 二、功能需求

### 2.1 首页 - 家具分类管理

**功能说明：**
- 预置 5 个分类：床、沙发、衣柜、电视柜、桌子
- 每个分类显示为大卡片 + 图标 + 名称
- 支持用户自定义添加新分类
- 支持删除分类（预置分类也可删除）
- 删除非空分类时弹出确认："将同时删除该分类下所有风格和照片"
- 点击分类进入该分类下的风格列表

**预置分类图标（Lucide Icons）：**
| 分类 | 图标 |
|------|------|
| 床 | Bed |
| 沙发 | Armchair |
| 衣柜 | Warehouse |
| 电视柜 | Tv |
| 桌子 | Table |

### 2.2 风格列表

**功能说明：**
- 展示当前分类下的所有风格
- 每个风格条目显示：风格名称 + 该风格下的照片数量
- 支持添加自定义风格（输入风格名称）
- 支持删除风格（删除时级联删除该风格下所有照片）
- 点击风格进入照片详情页

### 2.3 照片详情页

**功能说明：**
- 3 列网格布局展示所选风格下的所有照片
- 照片按原始比例展示（等宽不等高）
- 提供上传按钮，点击弹出选择："拍照" 或 "从相册选择"
- 支持相册批量选图上传
- 上传时前端自动压缩（宽度 800~1200px，质量 0.7~0.8）
- 支持照片删除（带确认弹窗）
- 每张照片下方紧跟备注输入框

### 2.4 备注功能

**交互规范：**
- 备注框位于照片正下方
- 细边框（#333）、深灰文字（#666）
- 点击直接编辑，失焦后自动保存
- 备注内容：型号、价格等文字信息
- 纯文本格式

### 2.5 照片全屏查看

**功能说明：**
- 点击照片进入全屏查看模式
- 支持左右滑动切换照片
- 提供关闭按钮退出全屏

### 2.6 分享系统

**分享方操作：**
- 在风格列表或照片详情页可触发分享
- 弹出底部分享面板：显示分享链接 + 复制按钮 + 系统分享（其他 App）
- 分享粒度：按"风格"分享
- 可设置过期时间（选项：1天 / 7天 / 30天 / 永久）
- 可取消/撤销已有分享链接

**被分享者体验：**
- 通过链接进入，无需登录
- 自动进入只读模式
- 隐藏所有编辑操作（上传、删除、备注编辑、添加风格/分类）
- 不显示照片备注（备注为私密信息）
- 照片可点击全屏查看

### 2.7 数据存储

- 全局共享数据，无用户隔离
- 照片存储在 Supabase Storage
- 照片与备注信息正确关联
- 删除照片时同步清理 Storage 中的文件

---

## 三、数据模型

### categories（家具分类）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | serial | 主键 |
| name | varchar | 分类名称 |
| icon | varchar | 图标名称（Lucide Icon name） |
| sort_order | integer | 排序序号 |
| is_preset | boolean | 是否预置分类 |
| created_at | timestamp | 创建时间 |

### styles（风格）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | serial | 主键 |
| category_id | integer | 所属分类（FK → categories.id） |
| name | varchar | 风格名称 |
| created_at | timestamp | 创建时间 |

### photos（照片）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | serial | 主键 |
| style_id | integer | 所属风格（FK → styles.id） |
| image_url | text | 照片公网访问 URL |
| image_key | varchar | Storage 中的存储路径（用于删除时清理） |
| note | text | 备注内容（可为空） |
| created_at | timestamp | 创建时间 |

### shares（分享记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | serial | 主键 |
| share_token | varchar(32) | 分享链接唯一标识（UUID） |
| style_id | integer | 分享的风格（FK → styles.id） |
| expired_at | timestamp | 过期时间（NULL 表示永久） |
| created_at | timestamp | 创建时间 |

---

## 四、页面结构与导航

### 底部 Tab Bar（3 个 Tab）

| Tab | 页面 | 图标 | 说明 |
|-----|------|------|------|
| 首页 | 家具分类列表 | Home | 5 个预置分类 + 自定义分类 |
| 分享 | 分享中心 | Share2 | 查看他人分享的内容（只读） |
| 我的 | 个人中心 | User | 分享管理、退出 |

### 导航流程

```
首页（分类列表）
  └→ 风格列表（某分类下）
       └→ 照片详情页（某风格下，3列网格 + 备注）
            └→ 全屏查看（点击照片）

分享 Tab
  └→ 分享详情页（只读模式，通过 share_token 访问）

我的 Tab
  └→ 分享管理列表（查看/取消自己的分享）
```

---

## 五、UI 设计规范

### 5.1 风格定义：暖白居家风

**气质与意象：**
- 关键词：温馨、自然、居家、舒适
- 意象：午后阳光透过亚麻窗帘洒在木质地板上，空气中弥漫着淡淡的木香

### 5.2 配色方案

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 | #8B6F47 | 温暖木棕色 |
| 背景色 | #FAF7F2 | 暖白/米色 |
| 卡片背景 | #FFFFFF | 纯白 |
| 次要文字 | #8B8680 | 暖灰 |
| 主要文字 | #2C2420 | 深棕黑 |
| 边框/分割 | #E8E0D8 | 暖灰线 |
| 强调/操作 | #C4956A | 浅驼色 |
| 危险/删除 | #D45D5D | 柔红 |

### 5.3 核心样式

- 卡片圆角：16px
- 卡片阴影：柔和暖色阴影（非黑色）
- 按钮：圆角胶囊形，主色填充
- 输入框：浅灰背景 + 无边框，内嵌感
- 备注框：细边框 #333 + 深灰文字 #666（按需求）
- 图标风格：线性图标，线宽 1.5~2

### 5.4 布局规范

- 首页分类：大卡片网格（2列）
- 照片网格：3列等宽不等高
- 容器内边距：16~20px
- 卡片间距：12~16px

---

## 六、API 接口设计

### 分类管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/categories | 获取所有分类 |
| POST | /api/v1/categories | 创建分类 |
| DELETE | /api/v1/categories/:id | 删除分类（级联删除子数据） |

### 风格管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/categories/:categoryId/styles | 获取分类下的风格列表 |
| POST | /api/v1/categories/:categoryId/styles | 创建风格 |
| DELETE | /api/v1/styles/:id | 删除风格（级联删除照片） |

### 照片管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/styles/:styleId/photos | 获取风格下的照片列表 |
| POST | /api/v1/styles/:styleId/photos | 上传照片（multipart/form-data） |
| PATCH | /api/v1/photos/:id | 更新照片备注 |
| DELETE | /api/v1/photos/:id | 删除照片 |

### 分享管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/shares | 创建分享链接 |
| GET | /api/v1/shares | 获取所有分享列表 |
| DELETE | /api/v1/shares/:id | 取消分享 |
| GET | /api/v1/shares/:token | 通过 token 获取分享内容（公开接口） |

---

## 七、技术决策汇总

| 决策点 | 结论 |
|--------|------|
| 前端框架 | Expo 54 + React Native |
| 后端框架 | Express.js |
| 数据库 | Supabase (PostgreSQL) |
| 照片存储 | Supabase Storage |
| 用户体系 | 无，全局共享 |
| 照片压缩 | 前端 expo-image-manipulator，宽 800~1200px，质量 0.7~0.8 |
| 分享方案 | 分享链接（全平台通用） |
| 备注格式 | 纯文本 |

---

## 八、开发阶段规划

### P0 - 项目骨架与基础设施

| 序号 | 任务 | 说明 |
|------|------|------|
| 0.1 | 项目初始化 | coze init、目录结构、README |
| 0.2 | 数据库建表 | categories、styles、photos、shares 四张表 |
| 0.3 | 预置数据 | 写入 5 个预置家具分类 |
| 0.4 | 后端基础结构 | Express 路由框架、Supabase 连接配置 |
| 0.5 | 前端导航骨架 | 底部 Tab Bar（首页/分享/我的）+ 页面占位 |
| 0.6 | 对象存储配置 | Supabase Storage bucket 创建 |

### P1 - 核心 CRUD 功能

| 序号 | 任务 | 说明 |
|------|------|------|
| 1.1 | 分类管理（后端） | GET/POST/DELETE 分类接口 |
| 1.2 | 分类管理（前端） | 首页大卡片展示 + 添加/删除交互 |
| 1.3 | 风格管理（后端） | GET/POST/DELETE 风格接口 |
| 1.4 | 风格管理（前端） | 风格列表页 + 添加/删除交互 |
| 1.5 | 照片上传（后端） | 接收文件 → 压缩 → 上传 Storage → 写入数据库 |
| 1.6 | 照片上传（前端） | 拍照/相册选择 → 前端压缩 → FormData 上传 |
| 1.7 | 照片展示（前端） | 3 列网格布局 + 等宽不等高 |
| 1.8 | 照片删除 | 前后端删除接口 + 确认弹窗 + Storage 清理 |
| 1.9 | 备注功能 | 照片下方备注框 + 失焦自动保存 |
| 1.10 | 全屏查看 | 点击照片全屏查看 + 左右滑动切换 |

### P2 - 分享系统

| 序号 | 任务 | 说明 |
|------|------|------|
| 2.1 | 创建分享（后端） | 生成 share_token + 过期时间 |
| 2.2 | 分享面板（前端） | 底部弹出面板：链接 + 复制 + 系统分享 |
| 2.3 | 分享页面（前端） | 通过 token 访问，只读模式，隐藏编辑，不显示备注 |
| 2.4 | 分享管理（前端） | "我的"页面中查看/取消分享 |
| 2.5 | 过期处理 | 过期链接不可访问，展示提示 |

### P3 - 体验优化

| 序号 | 任务 | 说明 |
|------|------|------|
| 3.1 | 空状态提示 | 无分类/无风格/无照片时的引导提示 |
| 3.2 | 加载状态 | 图片加载占位 + 骨架屏 |
| 3.3 | 操作反馈 | 上传进度、删除成功提示 |
| 3.4 | 路由检查 | app_check 路由一致性验证 |

---

## 九、确认清单

| 项目 | 状态 |
|------|------|
| 无登录功能，全局共享数据 | ✅ 已确认 |
| 分享粒度：按"风格"分享 | ✅ 已确认 |
| 分享页面不显示照片备注 | ✅ 已确认 |
| 点击照片全屏查看 | ✅ 已确认 |
| 删除分类弹窗确认级联删除 | ✅ 已确认 |
| UI 风格：暖白居家风 | ✅ 已确认 |
| 分享链接设置过期时间 | ✅ 已确认 |
| 照片压缩：前端处理，宽 800~1200px，质量 0.7~0.8 | ✅ 已确认 |
| 照片存储：Supabase Storage | ✅ 已确认 |
| 底部 Tab：首页 / 分享 / 我的 | ✅ 已确认 |
